/**
 * 
 * Exercise 1
 * Define una interfaz `Product` con las siguientes propiedades:
 * - `name` de tipo `string`
 * - `price` de tipo `number`
 * - `description` de tipo `string`
 * - `category` de tipo `string`
 * 
 * Define un array con 3 productos distintos.
 * 
 */






/**
 * TODO:
 * Exercise 2
 * Define una interfaz addicional `Service` con las propiedades
 * customerName: string   OK
 * price: number   OK
 * periodicity: string   OK 
 * 
 * Define una función `findCheap` que reciba un array de tipo Service y devuelva el elemento más barato.
 * !NO! Implementa esa función para buscar el elemento más barato de una lista de Products y el más barato de una lista de Services.
 */